services:
  listas:
    build: .
    image: listas:latest
    container_name: listas
    ports:
      - "8000:80"
    depends_on:
      - mariadb
    networks:
      - listas-network
    volumes:
      - ./:/var/www/html
    environment:
      HOME: /tmp
    user: "${UID:-1000}:${GID:-1000}"

  mariadb:
    image: mariadb:11
    container_name: listas_mariadb
    restart: always
    environment:
      MYSQL_DATABASE: listas
      MYSQL_USER: listas
      MYSQL_PASSWORD: listas
      MYSQL_ROOT_PASSWORD: listas
    volumes:
      - mariadb_data:/var/lib/mysql
    networks:
      - listas-network

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: listas_phpmyadmin
    restart: always
    depends_on:
      - mariadb
    ports:
      - "8081:80"
    environment:
      PMA_HOST: mariadb
      PMA_PORT: 3306
      PMA_USER: root
      PMA_PASSWORD: listas
    networks:
      - listas-network

  selenium:
    image: selenium/standalone-chrome
    container_name: listas_selenium
    ports:
      - "7900:7900"
    networks:
      - listas-network
    shm_size: 2gb

  # No /etc/hosts do seu computador colocar a linha `127.0.0.1 auth.local`
  # particularidades do protocolo oAuth1 usado na USP
  senhaunica-faker:
    image: uspdev/senhaunica-faker
    container_name: listas_senhaunica-faker
    ports:
      - "3141:3141"
    environment:
          - APP_URL=http://auth.local:3141
    networks:
      listas-network:
        aliases:
          - auth.local

  mailpit:
    image: axllent/mailpit:latest
    container_name: listas_mailpit
    ports:
      - "1025:1025"   # SMTP
      - "8025:8025"   # Interface Web
    networks:
      - listas-network

networks:
  listas-network:

volumes:
  mariadb_data:
